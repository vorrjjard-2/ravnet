export const metadata = {
  title: "Pub",
  description: "Scientific publications and writing on ML systems, forecasting, and production engineering.",
};

export default function PubLayout({ children }: { children: React.ReactNode }) {
  return children;
}
